package project;

public class Root  extends AEntry{
	
	/*let's build constructors for each class*/
	
	/**
	 * Constructor for the Root class
	 * In the root we don't have much, so we'll just set the type
	 * 
	 */
	public Root(){
		this.name = "root";
		this.type = EntryType.ROOT;
	}

}